import 'package:flutter/material.dart';

class SlotMasterScreen extends StatelessWidget {
  const SlotMasterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
